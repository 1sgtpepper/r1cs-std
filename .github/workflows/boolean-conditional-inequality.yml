name: Boolean conditional inequality reproduction

on:
  push:
    branches:
      - repro/boolean-conditional-inequality
  workflow_dispatch:

permissions:
  contents: read

jobs:
  reproduce:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CARGO_TERM_COLOR: always
      RUST_BACKTRACE: "1"
      AFFECTED_REVISION: 45e4e2697626a9f9481fe57e70ed29c687194102
      AFFECTED_TREE: b1d9b897751982714a94f4ca8844bca820f0a97a
      REPRODUCTION_DIR: reproductions/boolean-conditional-inequality
    steps:
      - name: Check out reproduction
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 2

      - name: Verify frozen base and isolated contents
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD~1)" = "$AFFECTED_REVISION"
          test "$(git show -s --format=%T HEAD~1)" = "$AFFECTED_TREE"
          test "$(git rev-list --count "$AFFECTED_REVISION"..HEAD)" -eq 1
          test -z "$(git diff --name-only "$AFFECTED_REVISION"..HEAD -- src)"
          diff -u \
            <(printf '%s\n' \
              .github/workflows/boolean-conditional-inequality.yml \
              reproductions/boolean-conditional-inequality/Cargo.lock \
              reproductions/boolean-conditional-inequality/Cargo.toml \
              reproductions/boolean-conditional-inequality/tests/boolean_conditional_inequality.rs | sort) \
            <(git diff --name-only "$AFFECTED_REVISION"..HEAD | sort)

      - name: Select Rust 1.85.1
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install 1.85.1 --profile minimal
          rustup default 1.85.1

      - name: Run isolated reproduction
        shell: bash
        working-directory: ${{ env.REPRODUCTION_DIR }}
        run: |
          set -euo pipefail
          {
            git -C ../.. show -s --format='commit=%H%ntree=%T%nparent=%P%ncommitted=%cI%nsubject=%s' HEAD
            git -C ../.. show -s --format='affected_commit=%H%naffected_tree=%T' HEAD~1
            rustc --version --verbose
            cargo --version --verbose
            sha256sum \
              Cargo.toml \
              Cargo.lock \
              tests/boolean_conditional_inequality.rs \
              ../../.github/workflows/boolean-conditional-inequality.yml
            cargo tree --locked
            cargo test --locked --test boolean_conditional_inequality -- --nocapture --test-threads=1
          } 2>&1 | tee reproduction.log

      - name: Preserve reproduction evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: boolean-conditional-inequality-evidence-${{ github.run_id }}
          path: |
            .github/workflows/boolean-conditional-inequality.yml
            reproductions/boolean-conditional-inequality/Cargo.toml
            reproductions/boolean-conditional-inequality/Cargo.lock
            reproductions/boolean-conditional-inequality/tests/boolean_conditional_inequality.rs
            reproductions/boolean-conditional-inequality/reproduction.log
          if-no-files-found: error
          retention-days: 30
